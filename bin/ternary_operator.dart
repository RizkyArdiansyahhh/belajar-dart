void main() {
  var nilai = 80;
  String status = nilai >= 75 ? "Lulus" : "Tidak Lulus";
  print(status);

  if (nilai >= 75) {
    status = "Lulus";
  } else {
    status = " Tidak Lulsus";
  }
  print(status);
}
